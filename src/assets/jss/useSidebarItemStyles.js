import { makeStyles } from '@material-ui/core/styles';

const useSidebarItemStyles = makeStyles(theme => ({
  link: {
    textDecoration: 'none',
    color: 'inherit'
  }
}));

export default useSidebarItemStyles;
